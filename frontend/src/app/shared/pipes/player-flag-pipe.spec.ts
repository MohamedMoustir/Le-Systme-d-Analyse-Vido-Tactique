import { PlayerFlagPipe } from './player-flag-pipe';

describe('PlayerFlagPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerFlagPipe();
    expect(pipe).toBeTruthy();
  });
});
