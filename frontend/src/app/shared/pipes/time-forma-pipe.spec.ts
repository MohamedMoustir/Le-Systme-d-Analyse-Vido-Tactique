import { TimeFormaPipe } from './time-forma-pipe';

describe('TimeFormaPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFormaPipe();
    expect(pipe).toBeTruthy();
  });
});
